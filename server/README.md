# server

Server-side implementation of Deklara. Not started — the project is in the
validation phase (see `docs/validation.md`); the product is built only if the
landing site in `web/` clears the decision thresholds.
